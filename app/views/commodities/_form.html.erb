<%= form_for @commodity do |f| %>
  <% if @commodity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commodity.errors.count, "error") %> prevented this commodity from being saved:</h2>
      <ul>
      <% @commodity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :commodity_types, class: "control-label" %><%= f.collection_select :commodity_type_id, CommodityType.all.order(:order_num), :id, :name, :prompt => "Select a commodity type" %>
  </p>

  <p>
    <%= f.label :name, class: "control-label" %>&nbsp;<%= f.text_field :name, class: "control-label" %>&nbsp;<%= f.text_field :name, class: "form-control typeahead edsUpdate tt-input", placeholder: "enter text here", required: "required", style: "display: inline-block; position: relative; vertical-align: top; background-color: transparent;", value: "" %>
  </p>

  <p>
    <%= f.label :galactic_average_price, class: "control-label" %>&nbsp;<%= f.number_field :galactic_average_price, class: "form-control typeahead edsUpdate tt-input", placeholder: "1", required: "required", style: "display: inline-block; position: relative; vertical-align: top; background-color: transparent;", value: "" %> cr
  </p>

  <p>
    <%= f.submit "Save", class: "btn btn-default" %>
  </p>
<% end %>