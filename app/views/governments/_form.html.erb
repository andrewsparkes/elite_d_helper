<%= form_for @government do |f| %>
  <% if @government.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@government.errors.count, "error") %> prevented this government from being saved:</h2>
    <ul>
    <% @government.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <table>
    <body>
      <tr>
        <td><%= f.label :name, class: "control-label" %></td>
        <td>&nbsp;&nbsp;&nbsp;</td>
        <td><%= f.text_field :name, class: "form-control typeahead edsUpdate tt-input", placeholder: "enter name here", required: "required", style: "display: inline-block; position: relative; vertical-align: top; background-color: transparent;", value: "" %></td>
        <td>&nbsp;&nbsp;&nbsp;</td>
        <td><%= f.submit "Save", class: "btn btn-default" %></td>
      </tr>
    </body>
  </table>
<% end %>