<%= form_for @economy do |f| %>
  <% if @economy.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@economy.errors.count, "error") %> prevented this economy from being saved:</h2>
    <ul>
    <% @economy.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <p>
    <%= f.label :name %>&nbsp;<%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>