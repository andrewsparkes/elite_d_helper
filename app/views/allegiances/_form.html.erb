<%= form_for @allegiance do |f| %>
  <% if @allegiance.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@allegiance.errors.count, "error") %> prevented this allegiance from being saved:</h2>
    <ul>
    <% @allegiance.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <p>
    <%= f.label :name %>&nbsp;<%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>